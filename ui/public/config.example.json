{
  "apiBaseUrl": "https://xxxx.trycloudflare.com",
  "_note": "Sửa ui/public/config.json (apiBaseUrl = link tunnel API, không / cuối), push main, đợi Actions deploy. Không dùng localhost — GitHub Pages không tới máy bạn."
}
